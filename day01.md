# Day1 of learning sql

# Topics of learning
- what is database?
- datatype(int,varchar,char,float,decimal)
- constraits (primary key, foreign key)
- create table ,create database,drop table,drop database
- diffrence between varchar(max), varchar(n)



# Notes
- database is a container where it holds objects like table,view and so on.
- sql is a structured query language what allow u to create or manupulate database of a system
- varchar(n) is used when u know the number of character mostly it used for name,email and so on But for varchar(max) its basically hold 2gb of space so its huge like it used for comment ,articles and so on for big amount of text

- char is used for fixed length of character like gender,country code

- differnce between primary key and foreign key is significant like primary key have to be one for one table but foreign key can be multiple for one table .